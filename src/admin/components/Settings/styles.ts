import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  root: {
    width: '60%',
    marginLeft: '200px',
  },
  title: {
    textAlign: 'center',
    fontSize: '35px',
  },
}));
