import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  root: {
    width: '1000px',
    marginLeft: '200px',
    textAlign: 'left',
  },
  title: {
    textAlign: 'center',
    fontSize: '35px',
  },
}));
