import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles(() => ({
  root: {
    width: '60%',
    margin: '0 auto',
  },
  title: {
    textAlign: 'center',
    fontSize: '35px',
    marginTop: '100px',
  },
}));
