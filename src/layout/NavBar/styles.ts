import { makeStyles } from '@material-ui/core';

export const useStyle = makeStyles(() => ({
  appBar: {
    backgroundColor: 'lightBlue',
  },
  buttonsContainer: {
    marginLeft: 'auto',
  },
  link: {
    textDecoration: 'none',
    color: 'black',
  },
}));
