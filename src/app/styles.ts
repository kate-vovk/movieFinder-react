import { makeStyles } from '@material-ui/core/styles';

export const useStyle = makeStyles(() => ({
  registerForm: {
    marginTop: 0,
    marginBottom: 0,
    marginLeft: 'auto',
    marginRight: 'auto',
    maxWidth: 700,
  },
}));
