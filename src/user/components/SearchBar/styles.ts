import { makeStyles } from '@material-ui/core';

export const useStyle = makeStyles(() => ({
  container: {
    display: 'flex',
    margin: '5px 0',
    marginLeft: 'auto',
  },
  searchField: {
    marginLeft: '5px',
  },
}));
