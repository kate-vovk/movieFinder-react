import { makeStyles } from '@material-ui/core';

export const useStyle = makeStyles(() => ({
  footer: {
    display: 'flex',
    backgroundColor: 'lightGrey',
  },
  favoritesButton: {
    color: 'yellow',
  },
}));
