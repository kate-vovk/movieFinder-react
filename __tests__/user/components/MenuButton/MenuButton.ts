// Temporary test, will be modified in the next PR
describe('user', () => {
  describe('components', () => {
    describe('MenuButton', () => {
      test('два плюс два равно четыре', () => {
        const test = 2 + 2;
        expect(test).toBe(4);
      });
    });
  });
});
